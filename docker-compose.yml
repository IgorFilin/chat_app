services:
  cabinet:
    build:
      context: ./personal_cabinet
      dockerfile: Dockerfile
    ports:
      - '8081:8081'
    depends_on:
      - backend

  backend:
    build:
      context: ./chat_api
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
